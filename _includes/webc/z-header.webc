<p>ho</p>
<p><slot></slot></p>
<style webc:scoped>
    :host p{
        display: block;
        padding: calc(var(--padding)/2) var(--padding);
        background-color: var(--cyan);
        color: var(--black);
    }

    :host *+* {
        margin-top: calc(var(--margin)/2);
    }

</style>