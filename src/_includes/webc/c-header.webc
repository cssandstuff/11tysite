<h1 class="c"><slot></slot></h1>
<style webc:scoped>
	:host .c{
		display: block;
		padding: calc(var(--padding)/2) var(--padding);
		background-color: var(--cyan);
		color: var(--black);
	}

	:host *+* {
		margin-top: calc(var(--margin)/2);
	}
</style>